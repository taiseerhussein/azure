---
- name: Create snapshots
  hosts: 192.168.63.1 # <<< IMPORTANT: Replace 'your_windows_group' with your actual inventory group name (e.g., 'windows_servers')
  # gather_facts: false
 
  tasks:

    - name: test ssh connection
      ansible.builtin.shell:
        cmd: ls -lrt
